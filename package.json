{
  "name": "@zhoro/ndce",
  "version": "0.0.1",
  "description": "A simple application to execute commands on network devices",
  "scripts": {
    "start": "ts-node src/start.ts",
    "clean": "tsc --build --clean",
    "build": "tsc --build"
  },
  "dependencies": {
    "debug": "^4.3.4",
    "telnet-client": "^2.0.8",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/debug": "^4.1.10",
    "typescript": "^5.2.2",
    "ts-node": "^10.9.1"
  }
}
