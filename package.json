{
  "name": "@zhoro/ndce",
  "version": "0.0.1",
  "description": "A simple application to execute commands on network devices and store the output in a database for later analysis.",
  "scripts": {
    "start": "ts-node src/start.ts",
    "clean": "tsc --build --clean",
    "build": "tsc --build"
  },
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "debug": "^4.3.4",
    "dotenv": "^16.3.1",
    "telnet-client": "^2.1.0"
  },
  "devDependencies": {
    "@types/debug": "^4.1.11",
    "@types/node": "^20.9.0",
    "prisma": "^5.5.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
